spring:
  application:
    name: fortune-service
security:
  basic:
    enabled: false
management:
  security:
    enabled: false
#  cloud:
#    config:
#      uri: ${vcap.services.config-server.credentials.uri:http://localhost:8888}
